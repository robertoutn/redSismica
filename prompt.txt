Crear un proyecto de software desde cero con las siguientes condiciones:
    lenguaje: Python 3.11.9 instalado,
    sistema operativo: Windows,
    IDE: Visual Studio Code,
    gestor de versiones: Git,
    gestor de base de datos: SQLite,
    gestor de entornos virtuales: venv,
    
    instalar sqlite3,
    configuración de la base de datos: SQLite,
    crear base de datos,

    instalar Django 4.2.5,
    framework: django configurado en español,
    estructura de carpetas: estándar de Django,

    idioma: preferentemente español,

    pruebas automatizadas o cobertura mínima: no,
    pruebas unitarias: no,
    pruebas de integración: no,

    integración continua: no,
    despliegue: no,

    documentación técnica: no,

    extraer y analizar todos los roles mencionados en los documentos 
        con autenticación de usuario y contraseña estándar de Django, 
        personalizar según los roles encontrados en los documentos, 
        estructura de las apps Django stándar, 
        nombre para la app principal de Django genérico,
    nombre para la app de administración de usuarios genérico,
    nombre para la app de administración de roles genérico,
    nombre para la app de administración de permisos genérico,
    
    proyecto para desarrollo local únicamente,
    
    interfaz de usuario: simple y funcional,
        el fondo de la interfaz de usuario debe estar relacionado con el tema de la aplicación,
    interfaz de usuario: web,

    poblar la base de datos con datos de prueba,

update: python.exe -m pip install --upgrade pip

El proyecto debe contener:
    - el caso de uso "Dar cierre a orden de inspección de ES" (Nro. 37)
    - un entorno virtual (venv) en la raíz del proyecto,
    - un archivo de configuración para el entorno virtual,
    - un archivo requirements.txt con las dependencias necesarias para el proyecto,
    - un archivo .gitignore para ignorar archivos innecesarios,
    - un archivo README.md con una breve descripción del proyecto,
    - un archivo manage.py para ejecutar comandos de Django,
    - un archivo de configuración para la base de datos,
    - un archivo de configuración para el entorno de desarrollo,

Extraer el texto y revisar la informacion contenida en los archivos tipo docx que estan en la carpeta documentos.
